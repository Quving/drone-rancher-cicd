kind: pipeline
name: pipeline
steps:
- name: publish
  image: plugins/docker
  settings:
    repo: pingu/drone-plugin-rancher-deploy
    tags: latest
    username:
      from_secret: docker_username
    password:
      from_secret: docker_password
  when:
    status: [ success ]
    branch: [ master ]

